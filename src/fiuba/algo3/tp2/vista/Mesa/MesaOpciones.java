package fiuba.algo3.tp2.vista.Mesa;

import javafx.scene.control.Button;
import javafx.scene.layout.HBox;
import javafx.stage.Stage;

public class MesaOpciones {
/*
    private Button cerrarButton;
    private Button ayudarButton;
    private Button crearArmaButton;
    private Button limpiarButton;

    private Stage mesaStage;

    public HBox opcionesBox;

    public MesaOpciones(Stage mesaStage){
        this.mesaStage = mesaStage;
        opcionesBox = new HBox(getCrearArmaButton(),getLimpirButton(),getCloseButton());
    }

    public HBox getVisual(){ return opcionesBox;}

    private Button getCloseButton(){
        cerrarButton = new Button();
        cerrarButton.setOnAction(e -> mesaStage.close());
        return cerrarButton;
    }

    private Button getLimpirButton(){
        limpiarButton = new Button();
        limpiarButton.setOnAction(e -> mesa.limpiar());
        return limpiarButton;
    }

    private Button getCrearArmaButton(){
        cerrarButton = new Button();
        cerrarButton.setOnAction(e -> mesa.crearArma());
        return cerrarButton;
    }
*/
}
